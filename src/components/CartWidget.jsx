import React from 'react';
import cart from '../image/cart.png';

const CartWidget = () => {
    return (
        <div>
            <img src={cart} alt="" />
        </div>
    )
}

export default CartWidget
